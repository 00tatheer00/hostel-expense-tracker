"use client";

import * as React from "react";

// Placeholder toast provider for Phase 1 UI shell
export function ToastProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
