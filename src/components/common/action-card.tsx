import * as React from "react";
import { QuickActionCard, QuickActionCardProps } from "./quick-action-card";

export function ActionCard(props: QuickActionCardProps) {
  return <QuickActionCard {...props} />;
}
